#!/bin/bash
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
PYTHON="$REPO_ROOT/.venv/bin/python"

if [ ! -f "$PYTHON" ]; then
    echo "错误：未找到虚拟环境 ($PYTHON)" >&2
    exit 1
fi

PYTHONPATH="$REPO_ROOT/src:$REPO_ROOT" exec "$PYTHON" -m hermes.platform.cli "$@"
